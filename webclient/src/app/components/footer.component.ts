import {Component} from '@angular/core';

@Component({
    selector: 'custom-footer', //"footer" is already a reserved keyword in HTML
    templateUrl: '../views/footer.component.html'
})
export class FooterComponent {

}